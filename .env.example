# Copy to .env (gitignored) and fill in. Required vars:

# Anthropic API key — required from Phase 1 onward.
ANTHROPIC_API_KEY=

# Optional overrides (defaults come from config/cowork-config.yaml):
# ALBUM_PIPELINE_CONFIG=config/cowork-config.yaml
# ALBUM_PIPELINE_LOG_LEVEL=INFO

# Phase 7+ (email integration):
# GMAIL_CLIENT_ID=
# GMAIL_CLIENT_SECRET=
# GMAIL_REFRESH_TOKEN=
# MISSIVE_API_TOKEN=
